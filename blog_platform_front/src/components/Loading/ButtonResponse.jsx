import React from 'react';

const ButtonResponse = ({buttonTitle}) => (
    <button className="btn btn-dark mt-2 mb-4 buttonResponse" disabled>{buttonTitle}</button>
);

export default ButtonResponse;